defaults:
  confidence: 50
  provider: pastebin.com
  tlp: amber

feed:
  rsa_dump:
    remote: http://pastebin.com/raw.php?i=yKSQd5Z5
    period: daily
    tags: botnet
    description: 'suspected apt'
    confidence: 75
    pattern: '^([a-zA-Z-\.]+\.[a-z]{2,3})'
    values: observable

